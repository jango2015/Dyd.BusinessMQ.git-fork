1.生产者批量发送消息，负载均衡测试。插入速度是否提升。
2.生产者某个数据节点出错，消息是否能够正常发送。数据节点恢复后，生产者（心跳时间）n秒后自动恢复。
3.生产者管理中心数据库出错，生产者是否正常发送消息。管理中心数据库恢复后，正常心跳。


0.消费者端多个业务订阅互不干扰验证。
1.消费者端负载均衡验证。
2.消费者端开启多线程并行消费，消息不会重复消费。
3.数据节点出错，消费者订阅报错。但是不影响其他分区节点正常消费，在数据节点恢复后，自动恢复。
4.消费者管理中心数据库出错，消费者消费暂停，并报错，管理中心数据库恢复后，所有消费者自动恢复。
5.消费者注册后关闭，生产者继续发送消息，消费者重启后，继续消费停止期间的消息。

1.所有节点出现不可恢复故障后，错误写入mq错误数据库，人工写代码进行恢复。
2.验证服务器时间（发送消息，消费者端）都以管理中心数据库为准。
3.redis 服务关闭，消费者端消息响应速度会延迟n秒，但是不会影响程序正常运行。且在redis服务恢复后，恢复正常。
4.消息未被标记已消费情况下，该分区的消息消费被终止，并报错。

